<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <td><a href = "/users/<%= current_user.id %>"><%= "#{current_user.first_name}" %> <%= "#{current_user.last_name}" %></td>
      </tr>
    </table>
  </div>
</div>
